<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profile page</title>
</head>

<body class="bg-zinc-900 p-9">
    <div class="container text-white ">
        <div class="logout text-right flex justify-between">
            <div class="flex gap-3">
                <div class="w-10 h-10 rounded-md">
                    <a href="/profile/upload"><img class="w-full h-full object-cover"
                            src='/images/upload/<%=user.profile%>' name="profile" alt=""></a>
                </div>
                <h1 class="text-3xl">Hello , <%= user.name%> 🖐
                </h1>
            </div>

            <a href="/logout" class="text-red-400">Logout</a>
        </div>
        <form action="/post" method="post">
            <textarea name="postcontent" placeholder="What's in your mind ?"
                class="resize-none w-1/3 bg-zinc-900 border border-2 border-slate-700 block mt-9 rounded-md min-h-[100px] outline-none p-3"></textarea>
            <input type="submit" value="Create Tweet"
                class=" px-5 py-2 rounded-md bg-blue-500 mt-2 text-sm cursor-pointer"></input>
        </form>

        <div class="posts mt-14 ">
            <p class=" mb-5 text-zinc-400">Your Tweets.</p>
            <div class="post-container">
                <% user.posts.forEach((post)=> { %>
                    <div class="post bg-zinc-800  w-1/3 rounded-md pt-5 pl-5 pr-5 pb-2 mb-3">
                        <p class="text-blue-400 select-none mb-2">@<%= user.name %>
                        </p>
                        <p class="text-sm tracking-tight text-zinc-300">
                            <%= post.postcontent %>
                        </p>
                        <div class="flex justify-between items-end">
                            <div class="btns mt-7 flex gap-7">
                                <a href="/like/<%= post._id %>" class="text-sm text-blue-500 select-none">Like</a>
                                <a href="/edit/<%= post._id %>" class="text-sm text-gray-400 select-none">Edit</a>
                            </div>
                            <div>
                                <a href="/delete/<%= post._id %>" class="text-sm text-red-400 select-none">Delete</a>
                            </div>
                        </div>
                        <p class="text-xs text-zinc-600 mt-1">
                            <%= post.date%>
                        </p>
                    </div>
                    <%}) %>
            </div>

        </div>
    </div>
</body>

</html>